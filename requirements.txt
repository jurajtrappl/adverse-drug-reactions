# Phase 1 pipeline (adr/, scripts/, tests/)
rdkit>=2024.3
scikit-learn>=1.4
lightgbm>=4.0
pandas>=2.0
numpy>=1.24
pytest>=7.0
matplotlib>=3.7   # scripts/plot_results.py
# Legacy 2023 notebook / we_network.py additionally need:
#   tensorflow<2.16 (uses tf.keras.preprocessing), SmilesPE, matplotlib
# Phase 2 (adr/nn.py, Mol2vec features)
torch>=2.2
chemprop>=2.1,<3
gensim>=4.3
